<Page
    x:Class="BrickBreaker.Play"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BrickBreaker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid x:Name="PlayPage1">
        <canvas:CanvasAnimatedControl x:Name="PlayPage" FontSize="14" Loaded="PlayPage_Loaded" Margin="0,-6,0,6" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Draw="Canvas_Draw" Update="Canvas_Update" />
        <Button x:Name="BackButton" Content="Back to Main Menu" HorizontalAlignment="Left" Margin="750,846,0,0" VerticalAlignment="Top" Background="#FFE2A332" Foreground="#FF000679" IsEnabled="False" Click="BackButton_Click"/>

        <Grid.Background>
            <ImageBrush Stretch="Fill" ImageSource="Assets/abstract-background.jpg"/>
        </Grid.Background>
    </Grid>
</Page>
